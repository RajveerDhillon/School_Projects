#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int main(int argc, char * argv[]){
	//int i;
	/*
	for (i = 0; i <= argc; i++) {
		printf("the inputs are: %c", argv[i]);
	}
	*/
	printf("The input is: %d \n", atoi(argv[]));
}
